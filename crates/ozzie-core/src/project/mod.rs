// Re-export from worm-project — the canonical source of truth.
pub use worm_project::{
    ExtractionHint, FsProjectRepository, InMemoryProjectRepository, ProjectLoadError,
    ProjectManifest, ProjectMemoryConfig, ProjectRegistry, ProjectRepository,
};
