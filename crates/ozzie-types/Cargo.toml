[package]
name = "ozzie-types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Ozzie Assistant — shared payload types (zero logic, zero infra)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
