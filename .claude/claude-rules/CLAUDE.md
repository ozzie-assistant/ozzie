# Claude Rules — Project Instructions

> For project vision and goals, see @README.md.

## Repository Structure

This repository is an inventory of reusable Claude Code assets:

- `rules/` — Reusable rule files, importable via `@` in any project
- `skills/` — Custom skill definitions for Claude Code
- `guidelines/` — Patterns and recommendations for working with Claude Code
